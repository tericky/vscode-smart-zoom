{
  "name": "vscode-smart-zoom",
  "displayName": "Smart Zoom",
  "description": "Automatically apply per-window zoom based on the current display",
  "version": "0.0.8",
  "publisher": "tericky",
  "author": "Tericky <tericky@gmail.com>",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/tericky/vscode-smart-zoom.git"
  },
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./out/extension.js",
  "scripts": {
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "test": "npm run compile && node --test \"out/**/*.test.js\"",
    "package": "npm run compile && npx --yes @vscode/vsce package --no-dependencies"
  },
  "contributes": {
    "configuration": {
      "title": "Smart Zoom",
      "properties": {
        "autoZoom.enabled": {
          "type": "boolean",
          "default": true
        },
        "autoZoom.pollInterval": {
          "type": "number",
          "default": 500,
          "minimum": 100,
          "description": "How often (ms) to check which display the window is on. Higher values use less CPU."
        },
        "autoZoom.stabilityChecks": {
          "type": "number",
          "default": 2
        },
        "autoZoom.defaultZoom": {
          "type": "number",
          "default": 0
        },
        "autoZoom.displayProfiles": {
          "type": "object",
          "default": {}
        },
        "autoZoom.zoomRules": {
          "type": "array",
          "default": []
        }
      }
    },
    "commands": [
      {
        "command": "autoZoom.enable",
        "title": "Smart Zoom: Enable"
      },
      {
        "command": "autoZoom.disable",
        "title": "Smart Zoom: Disable"
      },
      {
        "command": "autoZoom.detectCurrentDisplay",
        "title": "Smart Zoom: Detect Current Display"
      },
      {
        "command": "autoZoom.configureCurrentDisplay",
        "title": "Smart Zoom: Configure Current Display (Menu)"
      },
      {
        "command": "autoZoom.showStatus",
        "title": "Smart Zoom: Show Status"
      },
      {
        "command": "autoZoom.statusMenu",
        "title": "Smart Zoom: Status Menu"
      },
      {
        "command": "autoZoom.zoomInCurrentDisplay",
        "title": "Smart Zoom: Zoom In Current Display"
      },
      {
        "command": "autoZoom.zoomOutCurrentDisplay",
        "title": "Smart Zoom: Zoom Out Current Display"
      },
      {
        "command": "autoZoom.clearAllSettings",
        "title": "Smart Zoom: Clear All Learned Settings"
      }
    ]
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "@types/vscode": "^1.85.0",
    "typescript": "^5.3.3"
  }
}
