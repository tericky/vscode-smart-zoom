{
  "name": "per-window-zoom-spike",
  "displayName": "Per-Window Zoom Spike",
  "description": "Spike to verify feasibility of absolute and fractional per-window zoom in VS Code",
  "version": "0.0.1",
  "private": true,
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "spike.zoom.set",
        "title": "Zoom Spike: Set Current Window Zoom"
      },
      {
        "command": "spike.zoom.read",
        "title": "Zoom Spike: Read Current Window Zoom"
      }
    ]
  },
  "scripts": {
    "compile": "npx --yes --package typescript tsc --target ES2022 --module commonjs --strict --skipLibCheck --outDir out src/extension.ts"
  }
}
